node_modules/
.next/
out/
build/

# Base de datos y respaldos: son datos del negocio, no van al repositorio
data/*.db
data/*.db-shm
data/*.db-wal
respaldos/

# Material del cliente: su catalogo de prendas, las fotos y el logo.
# NO va a GitHub. Vive solo en la computadora donde se instala.
# Si hiciera falta rehacerlo:  npm run catalogo -- "catalogo.pdf"
public/

# Documentos internos del proyecto (entregables, prompts, notas)
docs/

# El PDF original del cliente: pesa 11 MB y ya esta procesado.
*.pdf

.env
.env.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
Thumbs.db
